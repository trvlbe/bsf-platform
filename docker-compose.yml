services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bsf_platform
      POSTGRES_USER: bsf
      POSTGRES_PASSWORD: bsf_dev
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
